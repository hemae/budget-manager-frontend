import {IProjectFormValues} from '../../interfaces'
import {ProjectPostBody} from '@shared/api'

export function mapProjectFormValuesToCreation(formValues: IProjectFormValues): ProjectPostBody {
    return formValues
}
